<div class="container">
  <h1>All Lists</h1>

  <% @lists.each do |list| %>
  <p><%= link_to list.name, list_path(list) %></p>
  <% end %>

  <p><%= link_to 'Create List', new_list_path %></p>
</div>
