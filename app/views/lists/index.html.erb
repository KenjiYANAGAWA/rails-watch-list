<div class="container">
  <h1>Movie Watch List</h1>

  <h2>My Lists in UL</h2>
  <ul>
    <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>


  <h2>My Lists in DIVs</h2>
  <div class="container">
    <% @lists.each do |list| %>
      <div class="item card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= list.photo.attached? ? cl_image_path(list.photo.key) : image_path('list_icon.jpg') %>)">
        <%= link_to list.name, list_path(list) %>
      </div>
    <% end %>
  </div>

  <div class="container">
    <%= link_to 'Create List', new_list_path, class: "btn btn-primary"%>
  </div>
</div>

  <div class="container">
  <h2>testing cloudinary</h2>
    <!-- app/views/articles/index.html.erb -->
    <%= cl_image_tag("qncvnbb0q6scqt8t9odp",
    width: 400, height: 300, crop: :fill) %>
    <!-- for face detection -->
    <%= cl_image_tag('p90pnp9nllsi01t0nlyd',
    width: 150, height: 150, crop: :thumb, gravity: :face) %>
  </div>
